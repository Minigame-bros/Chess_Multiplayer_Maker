<UserControl x:Class="ChessGame.Views.LanLobbyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800" Background="#222">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="SẢNH CHỜ MẠNG LAN" FontSize="32" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <Border Grid.Row="1" Background="#333" Padding="10">
            <ListBox ItemsSource="{Binding Rooms}" Background="Transparent" BorderThickness="0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#444" Margin="5" Padding="10" CornerRadius="5" Width="700">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBlock Text="{Binding DisplayText}" Foreground="White" FontSize="18" VerticalAlignment="Center"/>
                                <Button Grid.Column="1" Content="Tham gia" Command="{Binding DataContext.JoinRoomCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}" Padding="15,5" Background="#5cb85c" Foreground="White" FontWeight="Bold"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="Quay lại" Command="{Binding BackCommand}" FontSize="18" Padding="20,10" Margin="0,0,20,0" Background="#d9534f" Foreground="White"/>
            <Button Content="Tạo phòng mới" Command="{Binding CreateRoomCommand}" FontSize="18" Padding="20,10" Background="#0275d8" Foreground="White"/>
        </StackPanel>
    </Grid>
</UserControl>
