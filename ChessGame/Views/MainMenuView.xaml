<UserControl x:Class="ChessGame.Views.MainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800" Background="#222">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">
            <TextBlock Text="CỜ VUA" FontSize="48" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,50"/>
            
            <TextBlock Text="Nhập tên của bạn:" Foreground="LightGray" FontSize="16" Margin="0,0,0,5"/>
            <TextBox Text="{Binding PlayerName, UpdateSourceTrigger=PropertyChanged}" FontSize="18" Padding="5" Margin="0,0,0,30"/>
            
            <Button Content="Chơi 2 người (Cùng 1 máy)" Command="{Binding HotseatCommand}" FontSize="18" Padding="10" Margin="0,0,0,15" Background="#444" Foreground="White"/>
            <Button Content="Chơi qua mạng (LAN/WiFi)" Command="{Binding LanCommand}" FontSize="18" Padding="10" Background="#444" Foreground="White"/>
        </StackPanel>
    </Grid>
</UserControl>
