<UserControl x:Class="ChessGame.Views.InternetLobbyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ChessGame.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="#1A1A1A">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="600">
            <TextBlock Text="SẢNH CHƠI INTERNET" 
                       Foreground="White" 
                       FontSize="36" 
                       FontWeight="Bold" 
                       HorizontalAlignment="Center" 
                       Margin="0,0,0,20"/>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,20" HorizontalAlignment="Center">
                <TextBlock Text="Địa chỉ Máy chủ:" Foreground="White" FontSize="18" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox Text="{Binding ServerUrl, UpdateSourceTrigger=PropertyChanged}" Width="250" FontSize="18" Padding="5"/>
                <Button Content="Tải lại" Command="{Binding RefreshCommand}" Width="100" Height="35" Margin="10,0,0,0" FontSize="16"/>
            </StackPanel>

            <Border Background="#2A2A2A" CornerRadius="10" Padding="15" Margin="0,0,0,20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="300"/>
                    </Grid.RowDefinitions>
                    
                    <Grid Grid.Row="0" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Tên Phòng (Host)" Foreground="LightGray" FontWeight="Bold"/>
                        <TextBlock Grid.Column="1" Text="Số người" Foreground="LightGray" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Text="Thao tác" Foreground="LightGray" FontWeight="Bold" HorizontalAlignment="Center"/>
                    </Grid>

                    <ListView Grid.Row="1" ItemsSource="{Binding AvailableRooms}" SelectedItem="{Binding SelectedRoom}" 
                              Background="Transparent" BorderThickness="0" Foreground="White">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Width="530" Margin="0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="100"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <TextBlock Grid.Column="0" Text="{Binding HostName}" VerticalAlignment="Center" FontSize="16"/>
                                    <TextBlock Grid.Column="1" Text="{Binding PlayerCount}" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16"/>
                                    <Button Grid.Column="2" Content="Vào Phòng" 
                                            Command="{Binding DataContext.JoinRoomCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                            CommandParameter="{Binding}" 
                                            Width="90" Height="30" FontSize="14"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </Border>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Tạo Phòng Mới" Command="{Binding CreateRoomCommand}" Width="150" Height="40" FontSize="16" Margin="0,0,20,0"
                        Background="#4CAF50" Foreground="White" BorderThickness="0"/>
                <Button Content="Quay lại" Command="{Binding BackCommand}" Width="150" Height="40" FontSize="16"
                        Background="#F44336" Foreground="White" BorderThickness="0"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
