<UserControl x:Class="ChessGame.Views.BotSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">

    <UserControl.Resources>
        <!-- Animations -->
        <Storyboard x:Key="PanelEntrance">
            <DoubleAnimation Storyboard.TargetName="MainPanel"
                             Storyboard.TargetProperty="Opacity"
                             From="0.3" To="1" Duration="0:0:0.6"
                             EasingFunction="{x:Null}"/>
            <DoubleAnimation Storyboard.TargetName="PanelTranslate"
                             Storyboard.TargetProperty="Y"
                             From="40" To="0" Duration="0:0:0.6">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </UserControl.Resources>

    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard Storyboard="{StaticResource PanelEntrance}"/>
        </EventTrigger>
    </UserControl.Triggers>

    <Grid>
        <!-- Background -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#0A0A1A" Offset="0"/>
                <GradientStop Color="#151525" Offset="0.5"/>
                <GradientStop Color="#080815" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Grid x:Name="MainPanel" Opacity="1">
            <Grid.RenderTransform>
                <TranslateTransform x:Name="PanelTranslate" Y="40"/>
            </Grid.RenderTransform>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="40,30,40,20">
                <Button Command="{Binding BackCommand}" 
                        HorizontalAlignment="Left" VerticalAlignment="Center"
                        Background="Transparent" BorderThickness="0" Foreground="#8888AA"
                        FontSize="24" Content="🡠" Cursor="Hand" ToolTip="Quay lại">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <TextBlock Text="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}" FontSize="{TemplateBinding FontSize}"/>
                        </ControlTemplate>
                    </Button.Template>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Foreground" Value="#FFFFFF"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="CHỌN ĐỐI THỦ" FontSize="36" FontWeight="Black" Foreground="White" HorizontalAlignment="Center">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="#4444CC" BlurRadius="15" ShadowDepth="0"/>
                        </TextBlock.Effect>
                    </TextBlock>
                    <TextBlock Text="Hãy chọn một kì thủ máy để bắt đầu ván cờ" FontSize="16" Foreground="#AAAAAA" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                </StackPanel>
            </Grid>

            <!-- Bot Cards -->
            <ListBox Grid.Row="1" ItemsSource="{Binding Bots}" SelectedItem="{Binding SelectedBot}"
                     Background="Transparent" BorderThickness="0"
                     HorizontalAlignment="Center" VerticalAlignment="Center"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center" MaxWidth="950"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="10"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <ContentPresenter/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <RadioButton GroupName="BotSelection" 
                                     IsChecked="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}"
                                     Cursor="Hand">
                            <RadioButton.Template>
                                <ControlTemplate TargetType="RadioButton">
                                    <Border x:Name="CardBorder" Background="#16162C" BorderBrush="#2A2A4A" BorderThickness="2" CornerRadius="15" 
                                            Width="280" Height="360" Padding="20">
                                        <Border.Effect>
                                            <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="5" Opacity="0.5"/>
                                        </Border.Effect>
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="*"/>
                                            </Grid.RowDefinitions>

                                            <!-- Avatar -->
                                            <Ellipse x:Name="AvatarEllipse" Width="120" Height="120" HorizontalAlignment="Center" Margin="0,10,0,15" Stroke="#3A3A6A" StrokeThickness="3">
                                                <Ellipse.Fill>
                                                    <ImageBrush ImageSource="{Binding AvatarPath}" Stretch="UniformToFill"/>
                                                </Ellipse.Fill>
                                            </Ellipse>

                                            <!-- Name -->
                                            <TextBlock Grid.Row="1" Text="{Binding Name}" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                                            
                                            <!-- Elo -->
                                            <Border Grid.Row="2" Background="#252545" CornerRadius="5" Padding="8,4" HorizontalAlignment="Center" Margin="0,10,0,15">
                                                <TextBlock Text="{Binding Elo, StringFormat='Elo: {0}'}" FontSize="14" FontWeight="SemiBold" Foreground="#FFD166"/>
                                            </Border>

                                            <!-- Description -->
                                            <TextBlock Grid.Row="3" Text="{Binding Description}" FontSize="14" Foreground="#BBBBBB" TextWrapping="Wrap" TextAlignment="Center" VerticalAlignment="Top"/>
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="CardBorder" Property="BorderBrush" Value="#E6A817"/>
                                            <Setter TargetName="CardBorder" Property="Background" Value="#1E1E3A"/>
                                            <Setter TargetName="AvatarEllipse" Property="Stroke" Value="#E6A817"/>
                                            <Setter TargetName="CardBorder" Property="Effect">
                                                <Setter.Value>
                                                    <DropShadowEffect Color="#E6A817" BlurRadius="25" ShadowDepth="0" Opacity="0.6"/>
                                                </Setter.Value>
                                            </Setter>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="CardBorder" Property="Background" Value="#1A1A32"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </RadioButton.Template>
                        </RadioButton>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Start Button -->
            <Button Grid.Row="2" Command="{Binding StartGameCommand}"
                    HorizontalAlignment="Center" Margin="0,20,0,40"
                    Width="240" Height="60" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="BtnBorder" Background="#E6A817" CornerRadius="30">
                            <Border.Effect>
                                <DropShadowEffect Color="#E6A817" BlurRadius="15" ShadowDepth="0" Opacity="0.5"/>
                            </Border.Effect>
                            <TextBlock Text="BẮT ĐẦU VÁN CỜ" FontSize="18" FontWeight="Bold" Foreground="#111" 
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="#FFD166"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="BtnBorder" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.95" ScaleY="0.95" CenterX="120" CenterY="30"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Grid>
</UserControl>
